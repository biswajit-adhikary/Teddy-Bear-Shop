import React from 'react';
import './Cart.css';

const Cart = (props) => {
    console.log(props);
    return (
        <div>
            <h2>This is cart</h2>
        </div>
    );
};

export default Cart;